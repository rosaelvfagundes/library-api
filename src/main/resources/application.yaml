spring:
  application:
    name: library-api
  datasource:
    url: jdbc:postgresql://localhost:5432/library_db
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none