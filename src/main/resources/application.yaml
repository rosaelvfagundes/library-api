spring:
  application:
    name: library-api
  datasource:
    url: jdbc:postgresql://localhost:5432/library_db
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: none